// Legacy file, tests moved to config package
package cgpt

import (
	"testing"
)

// TestBackendDefaultModel is a legacy test, now moved to the config package.
func TestBackendDefaultModel(t *testing.T) {
	// Skip as test now lives in config package
	t.Skip("Test moved to config package")
}
