
goals-tools: .prompts-goals-tool
	./create-script goals-tool $<

ctx-exec: create-file
	./create-file ./ctx-exec ctx-exec wraps an arbitray exec and returns xml tagged content for llms including what the exec cmd was, exit code, and separated stdout/stderr. make the output nice and tidy
